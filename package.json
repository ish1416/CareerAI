{
  "name": "careerai",
  "version": "1.0.0",
  "description": "AI-Powered Resume Builder & Career Assistant",
  "scripts": {
    "start": "./deploy.sh",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ish1416/CareerAI.git"
  },
  "keywords": ["ai", "resume", "career", "builder"],
  "author": "CareerAI Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}